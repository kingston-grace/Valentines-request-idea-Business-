name = "valentine-chat"
main = "worker.js"
compatibility_date = "2024-01-01"

# Workers AI binding (uncomment if you have Workers AI enabled)
# [[ai]]
# binding = "AI"

# Environment variables are set in Cloudflare Dashboard
# PRIVATE_CONTEXT and SYSTEM_PROMPT should be set as secrets/env vars in Cloudflare

# Route configuration (set up in Cloudflare Dashboard)
# Route: phill-love-nyasha.lol/api/chat*
